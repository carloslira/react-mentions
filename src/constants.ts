export enum Key {
  Up = 'ArrowUp',
  Tab = 'Tab',
  Down = 'ArrowDown',
  Return = 'Enter',
  Escape = 'Escape',
}

export const DefaultTrigger = '@';

export const DefaultMarkupTemplate = '@[__display__](__id__)';
